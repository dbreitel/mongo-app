node_modules
dist
k8s/secret.yaml

# terraform state and vars hold generated passwords in cleartext
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# document the deliberate misconfigurations, the live IPs and the
# demonstration steps for real infrastructure, and this repo is public
terraform/README.md
terraform/eks-run.md
